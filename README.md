Efficient Parameterizations for Hierarchical Models

This case study revisits the question of how to parameterize a hierarchical model
in order to improve the computational performance of an MCMC sampler.

To render the quarto notebooks add [stan-dev/quarto-config](https://github.com/stan-dev/quarto-config) as a submodule.

